{
  "name": "motorbiketour",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "build": "node -e \"if(process.env.SUPABASE_URL&&process.env.SUPABASE_ANON_KEY){require('fs').writeFileSync('supabase-env.json',JSON.stringify({url:process.env.SUPABASE_URL,anonKey:process.env.SUPABASE_ANON_KEY}))}else{console.log('No Supabase env vars set, skipping config')}\"",
    "start": "npx serve . -l 3000 -c serve.json",
    "test": "npx playwright test",
    "deploy": "npx wrangler pages deploy . --project-name=motorbike-tour --branch=main"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@playwright/test": "^1.60.0",
    "undici": "^8.4.0"
  }
}
